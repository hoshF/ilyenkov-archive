---
title: "Source Text Cruxes For Later Print Verification"
created: "2026-07-27"
type: "note"
tags: ["translation", "source-criticism", "verification"]
language: "zh"
collection: "translation-workspace"
llm_wiki_eligible: "true"
gbrain_source: "project-markdown"
---

# 底本疑点清单（成书前核纸本用）

翻译过程中发现的、**疑为底本排印错或数字化讹误**的地方。处理原则是 DECISIONS 第 15、35 行：
无法确证为排印错的，**保留字面、不擅改、不加校勘译注**，只在此备案。

这份清单的用途只有一个：**将来取得 1997 纸本或《伊里因科夫文集》时，照单逐条复核。**
它是从各单元 `issues.md` 里汇总出来的——散在 40 个单元里没人找得到，
排版阶段需要的是一张表。**新发现的疑点：写进本单元 `issues.md` 之后，再补一行到这里。**

底本：`ilyenkov_markdown/.../knigi-dialektika-abstraktnogo-i-konkretnogo-*`（安德烈·迈丹斯基网站）。

## 〇、最高优先：本仓库的 markdown 转换丢了两个章标题（**不是底本失载**）

2026-07-29 查实：`ilyenkov_markdown/` 里 **ch003 与 ch024 的 markdown 缺了各自的章标题**，
而**底本网页上这两行是有的**。转换器在“一页有两个标题”（部分名＋章名）处只保留了第一个。

**先前本节把它记成“底本失载”，是错的**——那个判断建立在 `WebFetch` 的两次返回上，
而那两次返回的都是与本书无关的臆造内容（页面是 Windows-1251 编码，摘要模型在乱码上编话）。
所有者提供页面截图后才发现。**教训：抓取工具返回的内容若无法与已知事实对上，
必须当作未取到，不得据以下结论。** 改用 `curl` 取原始字节、自行按 windows-1251 解码即可。

从底本取回的两行（`1_1.html`、`4_1.html`）：

| 单元 | 底本网页上的章标题 | 中文 |
|---|---|---|
| ch003 | `Глава 1. Метафизическое и диалектическое понимание «конкретного»` | 第一章　对“具体”的形而上学理解与辩证理解 |
| ch024 | `Глава 4. «Конкретное» и диалектическое развитие` | 第四章　“具体”与辩证发展 |

同页丢失的还有**两个部的标题**（`Категории абстрактного и конкретного как категории
диалектической логики`、`Восхождение от абстрактного к конкретному как логическая форма,
соответствующая диалектике`）——这两条另有出处（index 页的目录，即 ch000 块 7 与块 28），
早已据以补入 `build_book.py`。

**波及范围**：41 个页面全部抓下，与库内 markdown **逐段**比对（不只比标题），
共查出 **6 处丢失、涉及 6 个单元**，其余 35 个单元一字未丢。

> **第一次排查只比了 `Глава N.`／`Часть N`，因此只查出 ch003／ch024 两处，
> 并据此宣称“只有这两个单元”——那个结论是错的。** ch002 的题词当时已在输出里出现，
> 被我当作解析偏移打发掉了。**教训：范围结论必须建立在全文比对上，
> 不能建立在只查了一类模式的扫描上。**

| 单元 | 页面上有、库内 markdown 没有 | 性质 | 位置 |
|---|---|---|---|
| ch001 | `[К немецкому изданию]` | 副标题 | 块 1 之后 |
| ch002 | 列宁题词 `Опираясь на то, как применял Маркс материалистически понятую диалектику Гегеля, мы можем и должны разрабатывать эту диалектику со всех сторон...` ＋署名 `В.И. Ленин` | **正文内容，须翻译** | 块 2 之后、正文之前 |
| ch003 | 部标题 `Категории абстрактного и конкретного как категории диалектической логики`＋`Глава 1. Метафизическое и диалектическое понимание «конкретного»` | 标题 | 块 1 之后 |
| ch024 | 部标题 `Восхождение от абстрактного к конкретному как логическая форма, соответствующая диалектике`＋`Глава 4. «Конкретное» и диалектическое развитие` | 标题 | 块 1 之后 |
| ch036 | `1. Категории абстрактного и конкретного в формальной логике`／`2. История понятий абстрактного и конкретного`／`3. Трактовка понятий абстрактного и конкретного в диалектической логике` | 节标题 ×3 | 分别在块 9、33、126 之前 |
| ch038 | `2. Созрело ли наше общество для научно-теоретического анализа?`／`3. Созрело ли наше общество для научно-теоретического исследования?` | 节标题 ×2 | 分别在块 66、71 之前 |

**补块的下游后果（2026-07-30 补记）**：章节因此不再能无损重建回 `source_snapshot`，
逐章的 `payload_sha256`／`payload_bytes` 与 `start_byte`／`end_byte` 都仍指原始切分。
`split_longform_markdown.py` 原先把这当故障并**在第一处就中断整个循环**——本作品在注册表里
排第 6，于是其后 9 部长篇自补块之日起从未被验证过（看着像「一部失败」，实为「九部没查」）。
现已改为：读到清单的 `post_split_corrections` 即知偏离有意，以 `file_sha256` 为当前状态的
完整性锚、以当前章节核对 `.fulltext`；且逐部收集错误、查完 15 部再汇总。
`.fulltext` 阅读副本已重新生成（1737124 → 1738019 字节）。**快照与 `original_sha256` 不动**。

**ch036 与 ch038 的节标题全丢／半丢，直接影响成书的附录结构**：
ch036 现在在书里是一篇无小节的长文（实为三节），ch038 现在只显出「1. 政治经济学与逻辑学」
一个孤零零的编号节（实为三节）。

**另有一处不是丢失，是讹字**：ch036 块 22 库内作 `но затрагивая`，底本网页作 `не затрагивая`
（一个字母，意思相反）。**两稿都译作“没有触及”，即按正确读法译出**，译文无需改动。
**已于 2026-07-29 改回 `не`**，只重取了 accuracy，language 保持有效——这正是第五节“乙”类
的实例：源文校订而中文不变，两个哈希的失效条件确实不同。

### 处理进度（2026-07-29）

**补回的是 8 个块**：ch001 一个、ch002 两个、ch036 三个、ch038 两个。
（当时的 prompt 与几条提交信息误写作「10 个块」，是把六处丢失的项数与块数混算了；
全书块数因此由 2994 变为 3002，不是 3004。）

| 单元 | 状态 |
|---|---|
| ch001、ch002、ch036、ch038 | **已补入源文件与两稿**，锚点重编、两个哈希重取重签，`--check` 全绿 |
| ch003、ch024 | **仍只在 `build_book.py` 供给**，未进单元。补进去要再动 144 块锚点，收益仅是取消构建脚本里的 `open2` 特例，待定 |

补入后成书的变化：作者序的副标题〔为德文版而作〕并入标题（不再自成一节）、
序言卷首恢复列宁题词（右半栏小一号、署名右对齐，新增 `\bookepigraph`／`\bookepigraphby`）、
附录 I 恢复三节、附录 II.2 恢复第二三节，目录由 50 条增至 55 条。

**眼下处理**（2026-07-29）：章标题与部标题一样，由 `build_book.py` 的 `STRUCTURE` 供给，
成书里排在部分标题下方同页，与底本 `1_1`／`4_1` 两页同构。**源 markdown 未改动**——
改它会使 `source_sha256` 失效，且新增块会让该单元其后所有锚点整体位移
（ch003 19 块、ch024 125 块），代价远大于收益。

**留待的正事**：若将来要让源 markdown 恢复与底本一致，这是一项独立小工程——
补两个块、重编两单元的锚点、重取 accuracy 与 language 并重签（第五节“丙”类）。
中文译名（上表右列）届时应随之进入 ch003／ch024 的两稿，由起草方译、审计方核，
不再只存在于构建脚本里。

## 一、优先复核（影响译文取义）

| 单元 | 块 | 底本字面 | 疑为 | 为什么存疑 | 现译 |
|---|---|---|---|---|---|
| ch031 | 5 | `абстрактный историзм метода материалистической диалектики` | `конкретный` | 字面是“唯物辩证法方法的**抽象**历史主义”，**与本节首句“恰恰在于其具体性”表面抵触**。但有自洽读法（辩证法一旦被形而上学地运用也会滑入抽象历史主义），紧接下句“连黑格尔那样的辩证头脑也未能摆脱”正好印证 | 按字面 |
| ch035 | 116 | `реальная возможность` | `невозможность`（脱 `не`） | 上下文与紧接一句“不可能性就是不可能性”都指向“**不**可能性” | 按字面“现实可能性”，起草方未擅改，正确 |
| ch028 | 61 | `унаследована предками` | `потомками` | “由**祖先**继承”方向倒了；`предками` 有勉强读法（该祖先物种从它自己更早的祖先承下特征） | 按字面 |

## 二、注记归类待定（影响是否加“——俄文版编者”）

| 单元 | 块 | 底本 | 待定的是 |
|---|---|---|---|
| ch034 | 63 | `[цитировано по Марксу]`（麦克库洛赫“劳动”定义引文末） | **无 `Э.И.` 署名**，正文层面只能排除第五类。现按不带署名的〔转引自马克思〕处理；**若纸本显示系俄文版编者所加，须补“——俄文版编者”** |
| ch035 | 100 | `[цит. по Марксу]` | 与 ch034 块 63 同型，**两处必须同一处理** |

## 三、已按正确读法译出，仅备记（无需改动，核到时确认即可）

| 单元 | 块 | 底本 | 正确读法 |
|---|---|---|---|
| ch031 | 36 | `отравили`（毒化） | 疑 `отразили`（反映）。字面“毒化”亦通，故按字面译 |
| ch037 | 31 | `выразить это саморазвитие **о** развитии понятий` | `в развитии`（在概念的发展中），与紧接的 `в логике движения понятий` 平行 |
| ch038 | 52 | `вовсе не только и даже не столько специфика вообще диалектического развития действительности вообще` | 像未写完的对举前半句，缺 `сколько` 对项；手稿未整理，两稿按字面保留其未完成感 |
| ch040 | 24 | 列宁引文 `теория познания […], **взявшего** все ценное у Гегеля` | 属格形动词 `взявшего`／`двинувшего` 无中心词（前三个名词皆阴性主格），疑脱一属格名词（如 `материализма`）；已按“……的那个唯物主义的”读法译出 |
| ch041 | 31 | `та диалектика, в ходе которой такая вещь, как символ, знак, в качестве представителя реального золота, — которое…` | 主句谓语缺失（“符号、记号……作为现实金的代表者”后无动词）；已按代表链条的谓语关系译出 |
| ch035 | 71 | `неразрешенной в неразрешимую` | 按上下文读 |
| ch035 | 105 | `в разных отрешениях` | `отношениях` |
| ch035 | 225 | `вместе е тем` | `вместе с тем` |

## 四、非疑点，但会触发机器检查（免得将来重查一遍）

- **ch035 块 217**：`audit_unit.py` 的“引文起首”检查会报此块缺 2 个引号。已核：源文三处 «…» 中
  `«подводится»` 的引号在中文里省略了，属行文取舍，**不是丢引文**。36 个已审单元里唯一的假阳性。
- **ch007 块 10**：`check_book.py` 的“成对符号与源文不符”会报此块 `“”` 差 −1。已核（2026-07-29）：
  块 9—11 是黑格尔《谁在抽象地思维？》的跨块引文，**底本块 9 开 « 而不收 »**（俄文跨段引文只在末段收尾），
  块 11 另起一个 «…»。中文在块 10 末尾收了尾，**比底本更规整，不是缺陷**。
- **跨块引文普遍如此，不要逐个重查**：ch012 块 25、ch013 块 21、ch034 块 62、ch038 块 92
  四处译文都是“只开不闭”，而**底本对应块同样只开不闭**（«/» 计数各差 1），属照录，
  且合于中文跨段引文“每段起首用前引号、只在末段用后引号”的排印惯例。

## 五、核纸本的迁移方案（2026-07-24 定）

核纸本会改动俄文源文件，进而使 `translation.json` 里的哈希失效。**方案的要点是：
失效范围远比想象的小，而且两个哈希的失效条件不同。** 下面是实测过的机制，不是推测。

### 1. 机制：两个哈希，失效条件不同

| 哈希 | 覆盖范围 | 何时失效 |
|---|---|---|
| `source_sha256` | 俄文源文件全文（含 front matter） | **源文件一改就失效**，`--check` 报 `source SHA-256 mismatch` |
| `accuracy_scope_sha256` | `literal.md`＋`final.md`＋`issues.md`，**外加 `source_segments` 这段 JSON（内含 `source_sha256`）** | **源文件一改就连带失效**——这是设计如此：准确性是对着某一版源文说的 |
| `language_scope_sha256` | **只有 `final.md`** | **只在中文改动时失效**；源文校订而译文未变，语言审校依然有效 |

**推论（关键）**：源文校订若不改中文，**只需重取 accuracy，language 不动**。

**审校范围是按单元算的**（`check_translations.py` 的注释明写：
“Review scope is per unit so registering new units never invalidates completed reviews of other units”），
所以**未被校订的单元一个都不受影响**。

### 2. 范围：8 个单元，不是 42 个

本文件登记的 12 处疑点集中在
**ch028、ch031、ch034、ch035、ch037、ch038、ch040、ch041** 八个单元
（847 块，占全书 28%）。**其余 34 个单元与核纸本无关，哈希不动、不必重签。**

### 3. 三种结果，三种处理

核每一处疑点，只会得到三种结果之一：

| 结果 | 处理 | 代价 |
|---|---|---|
| **甲：纸本与数字化本一致**（疑点是底本原有的，不是转换错误） | **不动源文件**，只在本表该行注明“已核，纸本同”。**零哈希失效** | 最低 |
| **乙：纸本不同，但中文不必改**（多为第三类——译文本就按正确读法译出，如 ch037 块 31、ch035 块 105／225） | 改源文件 → 重取 **accuracy** 并重签；**language 保持有效** | 每单元一次 |
| **丙：纸本不同且中文须改**（第一类三处、第二类两处属此） | 改源文件 → 改译文 → **重跑 `audit_unit.py`、重写该单元 `issues.md`、两个哈希都重取并重签** | 最高，但只涉及个别单元 |

**第三类七处疑点（ch031 块 36、ch035 块 71／105／225、ch037 块 31、ch038 块 52、ch040 块 24、
ch041 块 31）译文已按正确读法译出**，若纸本证实，属“乙”——校订后源文与译文反而更贴合，中文不动。

### 4. 执行顺序

1. **先只核这 12 处**，不做全书通校。逐处填“甲／乙／丙”结论到本表。
2. 按结论分别处理：甲不动；乙重取 accuracy；丙走完整重审。
3. 每一次源文件改动，**front matter 的 `text_status` 与 `provenance` 须同步更新**
   （档案政策见 `notes/PHILOSOPHY_SOURCE_FORMAT_POLICY.md`“Verified OCR Admission”一节：
   已核文本须保留 source hash、reviewer、date、scope 与 provenance）。
4. 重签时**仍适用代填声明规则**（HANDOFF 第一节）：Claude 代填后须逐单元声明，待所有者认可。

### 5. 一个必须避开的做法

**不要为了“把全书标成已校订”而批量修改 42 个源文件的 `text_status`。**
`text_status` 在 front matter 里，**改它就改了 `source_sha256`**，
于是 **42 个单元的 accuracy 全部失效**——包括 34 个根本没被校订过的单元。
换来的只是一个档案状态字段，代价是全书重签。

若将来确实要把全书升格为“已人工核校”，那是一项**独立的通校工程**
（2994 块逐块比对纸本），应当单独立项、单独走审校，**不能作为核这 12 处疑点的副产品顺手做掉**。
